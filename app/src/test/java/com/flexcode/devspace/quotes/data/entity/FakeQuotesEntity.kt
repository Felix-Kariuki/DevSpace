package com.flexcode.devspace.quotes.data.entity

import com.flexcode.devspace.quotes.data.local.entity.QuotesEntity

val fakeQuotesEntity = listOf(
        QuotesEntity(
            author = "Felix",
            en = "If it works don't touch it",
            id = "5555"
        )
    )
